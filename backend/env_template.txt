# Скопируйте этот файл в .env и заполните значениями
# В Windows PowerShell: copy env_template.txt .env
# В Linux/Mac: cp env_template.txt .env

# Database Configuration
# Используйте отдельные переменные (рекомендуется)
# ВАЖНО: Замените DB_PASSWORD на ваш реальный пароль PostgreSQL!
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fbank

# Secret Keys
# ВАЖНО: Измените эти значения на случайные строки в production!
# Можно сгенерировать случайные строки командой: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-in-production
JWT_SECRET_KEY=jwt-secret-key-change-in-production

